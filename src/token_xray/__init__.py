"""token-xray: local-first, zero-network analyzer for LLM API spend exports.

The package makes no network calls. Every output artifact contains aggregate
statistics only; raw prompt text is never retained past parse time.
"""

__version__ = "0.1.0"
